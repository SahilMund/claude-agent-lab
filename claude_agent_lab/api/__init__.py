"""HTTP API for the dashboard (Phase 8) — a second way to see the same
backend the CLI (main.py) already drives, not a second implementation.
Every route here calls the exact functions main.py's REPL calls.
"""
